# Secrets et config locale
.mcp.json
.env
**/.env
*.local
.claude/*.bak

# MCP llm-copains — dumps auto des appels LLM créatifs (filet de sécurité, voir infra/mcp/MODELS.md)
infra/mcp/logs/

# Dépendances et builds
node_modules/
dist/
_site/
.vite/

# OS
.DS_Store
Thumbs.db

# Génériques (D-007 / D-008, refonte GED 2026-09-12) : transit, brut, staging, artefacts de run
**/_new-*/
**/inbox/*.mp3
**/.build-*/
**/*.log
**/.artifacts/
**/_scratch/
temp/
*.zip

# Zones locales dino non déployées (bruts HD, tests)
site/img/dinos/brutes/
site/img/dinos/coloriage-test/

# Worktrees d'agents en arrière-plan (Agent tool isolation:worktree)
.claude/worktrees/

# Payload hook Stop Kimi Code (runtime)
.kimi-code/hooks/stop-payload.jsonl

# Referentiel de contenu — dumps machine regénérables (le tableau de bord _ETAT-CONTENU.md
# et la base d'empreintes empreintes.json SONT versionnés — ne pas les ignorer)
studio/referentiel/registre.json
studio/referentiel/plan-generation.json
studio/referentiel/textes-jeux.json
studio/referentiel/generer/textes-jeux.json
