# studio/minijeux/INBOX.md — Bot Telegram + Digests Claude

> Dépôt automatique : bot Telegram + digests Claude.
> Dépôt manuel de fichiers : [`inbox/`](inbox/) (répertoire).
> `game-pmo` checke ce fichier ET `inbox/` à chaque session.
> Sections passées (distillées) : [`memory/archive/INBOX-2026.md`](memory/archive/INBOX-2026.md).

Convention de section :

```
## YYYY-MM-DD — sujet libre
[contenu brut]
```

Une section distillée est remplacée par `> ✅ Distillé → [fichier cible]` puis déplacée dans l'archive ci-dessus une fois la session close.
