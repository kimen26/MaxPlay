# 🔒 MJ-38 — Saute-mouton ! — Décisions figées

> Validé Papa Yann **2026-08-01** (mutualisation UI écran de victoire, directive transverse).
> Chaque ligne 🔒 fait **LOI**. Toute ligne ❌ 🔒 = régression interdite.

## Méca (rappel)
Dames solo (Max saute par-dessus les dodos pour les faire disparaître). 9 niveaux précalculés (BFS), 3 tiers ★/★★/★★★.

## Écran de fin 🔒
- 🔒 **Un seul écran de victoire standard** : `G.showEnd({replayUrl:'mj-38.html'})` au dernier niveau (mj-golden.js / css/mp-theme.css). **Aucun overlay maison** (`#win-overlay`/`.ov-box` etc. supprimés 2026-08-01, ne jamais réintroduire).
- 🔒 **Fin de manche intermédiaire** (niveau 1..8 sur 9) : célébration `MaxFX.finalStar(..., {style:'breathe', label:'BRAVO !'})` (bibliothèque `celebrations.js`), puis **enchaînement automatique** vers `nextLevel()` après un court délai — **pas de bouton "Continuer" local**.
- ❌ 🔒 Ne pas recréer un `<div class="overlay">`/`.ov-title`/`.ov-text`/`.ov-btn` maison pour la fin de manche ou de partie.

## Test
`studio/minijeux/tests/mj-38.spec.mjs` — vérifie l'absence de `#win-overlay` dans le DOM et le chargement automatique du niveau 2 après la victoire du niveau 1 (sans clic sur un bouton local). Vert avant push.
