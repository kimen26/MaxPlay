# tests/_scratch — scripts jetables archivés (2026-07-15)

48 scripts `_*.mjs` de debug one-shot (sondes Grok/GPT DOM, download avatars, diagnostics
génération d'images) déplacés ici depuis `tests/` lors du scan militaire 2026-07-15 pour
dégager le harnais réel (run.mjs, run-all.mjs, audit-gabarit.mjs, mj-XX.spec.mjs).

**Rien n'est perdu** : ce sont des outils d'exploration passés, conservés pour référence.
Aucun n'est importé par le harnais (`run.mjs` ne charge que `mj-XX.spec.mjs`). Réutilisables
à la main si besoin de re-sonder un service externe.
